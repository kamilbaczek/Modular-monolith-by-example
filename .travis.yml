language: csharp
mono: none
dist: xenial
dotnet: 5.0
install:
  - dotnet restore
script:
  - dotnet build
  - dotnet test -v=normal

deploy:
  provider: heroku
  api_key: $Heroku__App__Key
  app: estimation-tool-kb
  on:
    branch: develop
notifications:
  slack: estimationtool:$Slack__Key
